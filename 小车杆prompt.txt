在该目录下开发一个基于gymnasium的小车杆rl算法测试项目，基于我的命令一步一步将它开发完整。项目的框架是fram_work是项目的中心，运行小车杆环境；utils是工具类，负责提供可能的公共工具方法给其他类调用；sarsa、gradient、Q_learning是算法类，负责储存更新每轮训练更新策略的原则和策略。
我希望项目实现的功能是：每次重启程序前，我可以在代码内部手动更改参数来选择调用哪个算法类来进行训练。每次程序开始运行时，均采用随机策略，随着训练的进行，根据选择的算法来逐渐更新策略。我是处于学习目的设计这个项目的，所以要尽可能多写注释，并明确在实现必要功能的基础上，代码简单、可读性强的重要性大于添加额外功能的重要性的原则。
并且我不希望你一次性写完代码，而是根据我的指令一步一步实现。
fram_work的功能：初始化好环境之后，进入n轮循环，每轮循环进行一次测试，记录奖励，最后绘制奖励关于n的函数的折线图，n我也能在代码内部手动更改。
三个算法类的作用：保存小车的策略，在fram_work每轮循环时获取其传过来的环境参数，根据自身策略选择小车的动作返还给fram_work。

现在实现第一步：
完成fram_work。小车的策略由随即动作代替算法类的决策。

现在实现第二步：
    完成sarsa类。循环中，每次调用sarsa，根据ε-greedy原则的参数ε(我在sarsa类的源代码可以手动设置)来选择是探索还是利益最大化。无论是哪种，都要更新策略相关的参数。sarsa更新参数的原则： Q(S,A)+α[R+γQ(S',A')-Q(S,A)]；
    fram_work类增加参数state,我可以在代码中手动更改；为0时就是已有的随机策略；为1时为sarsa策略；

1.策略梯度定理H_{t+1}(A_t) <- H_t(A_t) + α(R_t - R̄_t)(1 - π_t(A_t))
对已执行动作 A_t 的偏好更新（增加好动作的偏好）
H_{t+1}(a) <- H_t(a) - α(R_t - R̄_t)π_t(a)，对所有 a ≠ A_t
对未执行动作的偏好更新（降低相对偏好）
θ_{t+1} <- θ_t + α * G_t * ∇ln π(A_t|S_t, θ_t)
REINFORCE 更新规则，G_t 为从t开始的实际回报
2.Sarsa: Q(S,A)+α[R+γQ(S',A')-Q(S,A)]
3.Q-learning: Q(S,A)+α[R+γ·max_a Q(S',a)-Q(S,A)]